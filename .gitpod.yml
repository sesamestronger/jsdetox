image: remnux/jsdetox
# Command to start on workspace startup (optional)
tasks:
    - command: cd /home/nonroot/jsdetox && ./jsdetox -l $HOSTNAME 2>/dev/null 
# Ports to expose on workspace startup (optional)
ports:
  - port: 3000
    onOpen: open-preview